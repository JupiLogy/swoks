# How to run the sample experiments

Start in the root folder - the one that contains `swoks.py`.

Run `python -m sample_experiments.my_experiment` where `my_experiment` is the name of the file containing the experiment you want to run.